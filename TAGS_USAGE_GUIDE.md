# 🏷️ ctags 跳转功能使用指南

## 🚀 立即测试

1. **打开测试文件**：
   ```bash
   nvim test_lsp.py
   ```

2. **找到第27行的函数调用**：
   ```python
   hello_world()  # 将光标放在这里
   ```

3. **测试跳转**：
   - 按 `Ctrl+]` → 跳转到第17行的函数定义
   - 按 `Ctrl+o` → 返回到第27行

## 📋 可跳转的标签

### Python 函数 (test_lsp.py)
- `hello_world` (第17行) - 测试函数
- `calculate_sum` (第22行) - 计算函数
- `main` (第26行) - 主函数

### C++ 标签 (test_lsp.cpp)
- `Calculator` (第17行) - 类定义
- `add` (第24行) - 加法函数
- `multiply` (第30行) - 乘法函数
- `main` (第38行) - 主函数

## 🛠️ 常用命令

```vim
:tag hello_world    " 直接跳转到函数
:tags              " 显示跳转历史
:tselect main      " 选择多个 main 函数中的一个
:tnext            " 下一个匹配的标签
:tprev            " 上一个匹配的标签
```

## 🔧 项目配置

每个新项目都需要生成 tags：

```bash
# 标准生成命令
ctags -R --languages=C,C++,Python --exclude=.git .

# 或使用 Neovim 快捷键
# <leader>ct (空格+c+t)
```

## 📊 验证 tags 状态

```bash
# 检查 tags 文件
ls -la tags
wc -l tags

# 查看包含的函数
grep -E "(函数名)" tags
```

现在你的 ctags 跳转功能已经完全可用了！🎉