# Neovim 升级指南

## 🚀 升级到最新版 Neovim

当前你使用的是 Neovim 0.6.1，建议升级到最新版本以获得最佳体验。

## 📋 升级方法

### 方法 1: 使用升级脚本 (推荐)

```bash
cd /home/lucas/myVimrc
./build.sh --upgrade-neovim
```

脚本提供 4 种升级方式：

| 方法 | 优点 | 缺点 | 推荐指数 |
|------|------|------|----------|
| **Snap** | 自动更新、稳定 | 需要 sudo | ⭐⭐⭐⭐⭐ |
| **AppImage** | 便携、无需 sudo | 需要手动更新 | ⭐⭐⭐⭐ |
| **官方 PPA** | 系统集成度高 | Ubuntu 专用 | ⭐⭐⭐⭐ |

### 方法 2: 手动安装

#### 使用 Snap (推荐)
```bash
sudo snap install nvim --classic
sudo ln -sf /snap/bin/nvim /usr/local/bin/nvim
```

#### 使用 AppImage
```bash
curl -LO https://github.com/neovim/neovim/releases/latest/download/nvim.appimage
chmod +x nvim.appimage
sudo mv nvim.appimage /usr/local/bin/nvim
```

#### 使用官方 PPA
```bash
sudo add-apt-repository ppa:neovim-ppa/unstable
sudo apt update
sudo apt install neovim
```

## ✅ 验证升级

升级后运行：
```bash
nvim --version
```

应该看到类似输出：
```
NVIM v0.9.5
Build type: Release
LuaJIT 2.1.0-beta3
```

## 🔄 升级后重新配置

升级 Neovim 后，强烈建议重新运行配置脚本：

```bash
cd /home/lucas/myVimrc
./build.sh
# 选择 "2) 极致现代型 Neovim"
```

这会：
- 安装现代配置 (`init.vim`)
- 自动安装 LSP 服务器
- 配置 Treesitter 语法高亮
- 启用所有现代功能

## 🆚 版本对比

| 功能 | Neovim 0.6.1 | Neovim 0.9+ |
|------|---------------|-------------|
| **LSP 支持** | 基础 | 完整 |
| **Treesitter** | 实验性 | 稳定 |
| **Lua 配置** | 基础 | 完整 |
| **插件兼容性** | 限制 | 全面 |
| **性能** | 一般 | 优秀 |

## 🎯 升级后的新功能

升级到最新版本后，你将获得：

- ✅ **LSP 智能补全** - 真正的 IDE 级别代码补全
- ✅ **实时错误检查** - 代码问题即时发现
- ✅ **Telescope 搜索** - 强大的模糊搜索功能
- ✅ **Treesitter 高亮** - 更精确的语法高亮
- ✅ **现代 UI** - 美观的界面和状态栏
- ✅ **调试支持** - 内置调试器集成

## 🛠️ 故障排除

### 升级失败
如果升级过程中遇到问题：

1. **网络问题**：
   ```bash
   # 检查网络连接
   curl -I https://github.com
   ```

2. **权限问题**：
   ```bash
   # 确保有 sudo 权限
   sudo echo "权限测试"
   ```

3. **空间不足**：
   ```bash
   # 检查磁盘空间
   df -h
   ```

### 配置问题
如果升级后配置有问题：

1. **重置配置**：
   ```bash
   rm -rf ~/.config/nvim
   cd /home/lucas/myVimrc
   ./build.sh
   ```

2. **检查插件状态**：
   ```bash
   nvim +PlugStatus
   ```

3. **健康检查**：
   ```bash
   nvim +checkhealth
   ```

## 📞 获取帮助

如果遇到问题：
1. 查看完整文档：`cat NEOVIM_GUIDE.md`
2. 运行健康检查：`nvim +checkhealth`
3. 重新运行安装脚本

---

💡 **提示**: 升级后记得运行 `:Copilot setup` 配置 GitHub Copilot！